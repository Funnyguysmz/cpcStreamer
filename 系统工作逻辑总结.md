# 考试监控系统工作逻辑总结

## 一、客户端（desktop）工作逻辑

### 1. 客户端启动流程

1. 启动时创建主窗口，显示登录界面（login.html）
2. 用户输入服务器 IP、密码和机号
3. 登录成功后，进入考生登录界面（student-login.html）
4. 考生输入学号和姓名登录
5. 登录成功后，进入屏幕捕获界面（capture.html）
6. 客户端窗口自动最小化到托盘

### 2. 屏幕捕获与推流

1. 考生登录成功后自动开始捕获屏幕和摄像头（如果可用）
2. 分别推流屏幕和摄像头到不同的 RTMP 地址：
   - 屏幕流：`rtmp://服务器IP:1935/live/[streamKey]`
   - 摄像头流：`rtmp://服务器IP:1935/camera/[streamKey]`
3. 使用 FFmpeg 进行视频编码和推流
4. 将推流地址发送给服务器端

### 3. 系统监控与通知

1. 定期收集系统信息（MAC 地址、IP 地址、CPU 使用率等）
2. 定期发送系统信息到服务器
3. 定期检查服务器通知
4. 收到通知时通过对话框显示给考生

### 4. 安全措施

1. 禁用关闭快捷键（Alt+F4、Cmd+Q 等）
2. 退出应用需要管理员密码
3. 防止应用多开
4. 关闭窗口时默认最小化到托盘

### 5. 主要功能模块

- `createMainWindow()`: 创建主窗口
- `createTray()`: 创建托盘图标
- `initMediaServer()`: 初始化媒体服务器
- `startCapture()`: 开始捕获屏幕和摄像头
- `startRtmpStream()`: 开始 RTMP 推流
- `sendStreamInfoToServer()`: 发送流信息到服务器
- `startSystemMonitoring()`: 开始系统监控
- `checkServerNotifications()`: 检查服务器通知

## 二、服务端（d_service）工作逻辑

### 1. 服务器启动流程

1. 初始化服务器状态（客户端列表、WebSocket 连接等）
2. 启动 HTTP 服务器（默认端口 8080）
3. 提供静态文件服务
4. 启动 WebSocket 服务器
5. 启动定时清理离线客户端的协程

### 2. 客户端管理

1. 接收客户端报告的流地址和系统信息
2. 维护在线客户端列表
3. 定期检查客户端在线状态
4. 60 秒没有更新的客户端标记为离线

### 3. 流媒体管理

1. 接收并存储客户端的屏幕流和摄像头流地址
2. 提供 API 用于选择要显示的考生流
3. 支持通过 WebSocket 实时通知流选择变化
4. 维护"考生源"和"考生摄像头"两个场景

### 4. OBS 集成

1. 通过 WebSocket 与 OBS 通信
2. 提供 API 用于配置 OBS 集成
3. 支持在选择考生时自动切换 OBS 场景
4. 动态更新 OBS 场景中的媒体源 URL

### 5. 通知系统

1. 支持向特定客户端或所有客户端发送通知
2. 通知分级：info、warning、error
3. 存储通知历史记录
4. 提供 API 用于获取通知历史

### 6. 主要 API 端点

- `/api/report-stream`: 客户端报告流地址
- `/api/report-system-info`: 客户端报告系统信息
- `/api/clients`: 获取所有客户端信息
- `/api/select-stream`: 选择流
- `/api/selected-stream`: 获取当前选中的流
- `/api/set-output-url`: 设置输出流地址
- `/api/output-url`: 获取输出流地址
- `/api/send-notification`: 发送通知
- `/api/notifications`: 获取通知历史
- `/api/obs/config`: 设置/获取 OBS 配置
- `/ws`: WebSocket 连接处理

## 三、工作流程

1. 客户端启动并登录
2. 客户端报告流地址给服务器
3. 服务器记录客户端信息
4. 监考员/导播通过服务器管理界面选择考生
5. 服务器通知 OBS 切换场景
6. OBS 显示选中考生的画面
7. 导播通过 OBS 实现考场画面直播

## 四、OBS 设置步骤

1. 安装 OBS 和 obs-websocket 插件
2. 启用 WebSocket 服务器（默认端口 4455）
3. 创建两个场景：
   - "考生源"：添加媒体源，URL 由服务器动态更新
   - "考生摄像头"：添加媒体源，URL 由服务器动态更新
4. 在服务器管理界面配置 OBS 集成：
   - 启用 OBS 集成
   - 设置 WebSocket 地址（如 ws://localhost:4455）
   - 设置 WebSocket 密码（如果有）
