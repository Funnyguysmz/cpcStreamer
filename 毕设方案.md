# 毕设方案

## 客户端（必须实现）

基本要求：

1. 软件打开后，监考志愿者输入服务端部署的服务器ip地址以及访问服务器的验证密码，本机考试机号（如：14号机）；

2. 软件不可多开
3. 在不提供密码（目前方案为提供服务端指定的关闭密码）的情况下，软件不可被彻底关闭，例如无法通过杀进程情况下关闭
4. 优先适配Ubuntu 24.04

软件需要做的GUI页面：

1. 志愿者配置页面
2. 考生登录系统页面

注意事项：

1. 客户端通过服务器ip以及访问密码验证后，这些配置文件需要加密，考生或者其他使用者不可明文查看这些配置文件
2. 客户端通过服务器ip以及访问密码验证后，每15s定时扫描本机mac地址、ip地址，并向服务器汇报，其他能发送的各类基本数据（比如内存、cpu占用、进程列表之类的），这些额外信息由服务器设置是否“需要”，需要了才收集。
3. 考生登录上机考试系统（也就是本客户端）后，客户端在本地启动并维护一个直播服务器，并向服务器告知访问本流的地址；
4. 客户端如果需要修改基本要求里面1中所提到的配置文件，需要提供密码（此密码通过服务器验证）
5. 客户端维护的直播服务器最好同时包含屏幕直播以及摄像头直播流（如摄像头可用）
6. 当客户端登陆之后，需要随时可以接收来自服务器的消息
7. 所有消息通过弹窗告知
8. 客户端登陆之后最小化到托盘，如有退出或者修改配置文件需求，需要采用类似Windows下，从托盘处右键打开列表来选择功能

## 服务端

服务端需要分成两个功能：一个用于维护考试信息 一个维护直播推流

### 考试信息服务端

此服务端需要实现上述客户端的功能，此为次要。

**主要功能：**

1. 汇总我可访问的客户端直播流以及考生摄像头流
2. 当我选中某个流后，指挥维护直播推流的服务端，将接收流的地址切换到指定的地址

### 维护直播推流服务端

**主要功能：**

1. 接收来自考试信息服务端指定地址的直播流与指定的摄像头流
2. 维护一条推送至其他接收流客户端（如OBS studio）的流地址（此流地址不可变）